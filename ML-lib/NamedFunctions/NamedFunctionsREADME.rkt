#lang racket
#|NamedFunctions README

1:regular rackety functions
2:Dynamic scope for functions. (Function bodies are evaluated in the environment in which the are called)
3:Functions called with fewer arguments than defined have the remaining arguments assigned to undefined.
Functions called with more arguments than defined have the extra arguments evaluated and then ignored.

|#